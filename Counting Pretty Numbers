#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int T;
	cin>>T;
	
	for(int i=0;i<T;i++)
	{
	    int L,R;
	    cin>>L>>R;
	   
	    int count=0;
	    while(L!=R+1)
	    {
	        int i=L%10;
	        if(i==2||i==3||i==9)
	        {
	            count++;
	        }
	        L++;
	    }
	    cout<<count<<endl;
	}
	
	return 0;
}
